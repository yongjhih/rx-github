var chai = require('chai');
var expect = chai.expect;

var fetch = require('../index.js');
describe('rx-github', function() {

	before(function(done) {
	});

	after(function(done) {
	});

	it('should not empty repos', function() {
	});
});
